@model List<Web_Stadium.EFCore.ChuyenNhuong>
@{
    ViewData["Title"] = "Duyệt chuyển nhượng";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="background:var(--bg2);padding:100px 0 36px;border-bottom:1px solid var(--border)">
    <div class="container">
        <div class="section-label">Staff Dashboard</div>
        <h1 style="font-style:italic;line-height:0.9;margin:0">
            DUYỆT<br><span class="text-green">CHUYỂN NHƯỢNG</span>
        </h1>
    </div>
</div>

<div class="container py-4" style="max-width:860px">

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">✅ @TempData["Success"]</div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">❌ @TempData["Error"]</div>
    }

    @if (!Model.Any())
    {
        <div style="text-align:center;padding:80px 0;color:var(--muted)">
            <div style="font-size:3rem;margin-bottom:16px">✅</div>
            <div style="font-family:var(--ff-h);font-size:1.1rem">Không có yêu cầu chuyển nhượng nào cần xử lý</div>
        </div>
    }
    else
    {
        @foreach (var cn in Model)
        {
            var san = cn.DatSan?.KhungGio?.SanBong;
            var kg = cn.DatSan?.KhungGio;

            <div class="section-card p-0 overflow-hidden mb-4" style="border:1px solid var(--border);border-radius:12px">
                <!-- Header -->
                <div style="background:linear-gradient(135deg,#1d4ed8,#1e40af);padding:14px 18px;display:flex;justify-content:space-between;align-items:center">
                    <div style="color:#fff;font-weight:700;font-size:14px">📋 #@cn.Id — @cn.TieuDe</div>
                    <span class="badge bg-warning text-dark">Chờ Staff duyệt</span>
                </div>

                <div class="p-4">
                    <div class="row g-3 mb-3">
                        <!-- Thông tin sân -->
                        <div class="col-md-6">
                            <div style="font-size:11px;color:var(--green);letter-spacing:1px;margin-bottom:8px">🏟️ ĐƠN ĐẶT SÂN</div>
                            <table style="font-size:13px;width:100%">
                                <tr><td style="color:var(--muted);padding:2px 8px 2px 0;width:100px">Sân</td><td><strong>@(san?.TenSan ?? "—")</strong></td></tr>
                                <tr><td style="color:var(--muted);padding:2px 8px 2px 0">Ngày</td><td><strong style="color:var(--green)">@cn.DatSan?.NgayThiDau.ToString("dd/MM/yyyy")</strong></td></tr>
                                <tr><td style="color:var(--muted);padding:2px 8px 2px 0">Giờ</td><td>@kg?.GioBatDau.ToString("HH:mm") – @kg?.GioKetThuc.ToString("HH:mm")</td></tr>
                                <tr><td style="color:var(--muted);padding:2px 8px 2px 0">Loại</td><td>Sân @(san?.LoaiSan) người</td></tr>
                                <tr>
                                    <td style="color:var(--muted);padding:2px 8px 2px 0">Giá CN</td>
                                    <td>
                                        @if (cn.GiaChuyenNhuong == 0)
                                        {
                                            <span class="badge bg-success">MIỄN PHÍ</span>
                                        }
                                        else
                                        {
                                            <strong style="color:#f59e0b">@cn.GiaChuyenNhuong.ToString("N0") đ</strong>
                                        }
                                    </td>
                                </tr>
                            </table>
                        </div>

                        <!-- Thông tin hai bên -->
                        <div class="col-md-6">
                            <div style="font-size:11px;color:var(--green);letter-spacing:1px;margin-bottom:8px">👥 HAI BÊN</div>
                            <div style="font-size:13px;margin-bottom:8px">
                                <div style="color:var(--muted);margin-bottom:2px">Người chuyển (A)</div>
                                <strong>@(cn.UserA?.HoTen ?? "—")</strong>
                                <div style="font-size:12px;color:var(--muted2)">@(cn.UserA?.Email)</div>
                                <div style="font-size:12px;color:var(--muted2)">📞 @(cn.UserA?.SoDienThoai ?? "—")</div>
                            </div>
                            <div style="font-size:13px">
                                <div style="color:var(--muted);margin-bottom:2px">Người nhận (B)</div>
                                <strong>@(cn.UserB?.HoTen ?? "—")</strong>
                                <div style="font-size:12px;color:var(--muted2)">@(cn.UserB?.Email)</div>
                                <div style="font-size:12px;color:var(--muted2)">📞 @(cn.UserB?.SoDienThoai ?? "—")</div>
                            </div>
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(cn.MoTa))
                    {
                        <div style="font-size:13px;color:var(--muted2);font-style:italic;padding:10px;background:var(--bg3);border-radius:8px;margin-bottom:12px">
                            "@cn.MoTa"
                        </div>
                    }

                    <div style="font-size:12px;color:var(--muted);margin-bottom:16px">
                        🕐 Yêu cầu tạo lúc @cn.ThoiGianTao.ToString("dd/MM/yyyy HH:mm")
                    </div>

                    <!-- Form xử lý -->
                    <form method="post" action="/Staff/XuLyChuyenNhuong">
                        <input type="hidden" name="chuyenNhuongId" value="@cn.Id" />
                        <div class="mb-3">
                            <label class="form-label" style="font-size:13px;font-weight:600">Ghi chú (tùy chọn)</label>
                            <textarea name="ghiChu" class="form-control" rows="2" placeholder="Nhập ghi chú nếu cần..."
                                      style="background:var(--bg3);border-color:var(--border);color:var(--text);font-size:13px;resize:none"></textarea>
                        </div>
                        <div class="d-flex gap-3 flex-wrap">
                            <button type="submit" name="hanhDong" value="ChuyenChoOwner"
                                    class="btn btn-success rounded-pill px-4"
                                    onclick="return confirm('Chuyển hồ sơ này lên Owner để phê duyệt?')">
                                ✅ Chuyển lên Owner
                            </button>
                            <button type="submit" name="hanhDong" value="TuChoi"
                                    class="btn btn-danger rounded-pill px-4"
                                    onclick="return confirm('Từ chối yêu cầu chuyển nhượng này?')">
                                ❌ Từ chối
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        }
    }
</div>
